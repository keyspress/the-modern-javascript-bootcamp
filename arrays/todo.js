const todos = ['Get milk', 'Drink milk', 'Cry over having no milk']

console.log(`You have ${todos.length} things to do.`)

console.log(`Todo: ${todos[0]}`)

console.log(`Todo: ${todos[todos.length - 2]}`)